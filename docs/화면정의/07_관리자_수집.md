# 07. 화면: 수집 관리 · 수집 진행/결과

> 작성일: 2026-09-06 / 작성: designer
> 기획: `01_MVP_피아노_기획서.md` §3 F7, §5 관리자 예외(수집 관련 전부), §9 8-5(첫 배치 50곡)·8-10(2초 간격)·8-11(비피아노는 숨김)
> 공통: `00_공통_레이아웃_토큰.md` §3-14 관리 공통(ProgressBar, DataTable, AdminBanner, ConfirmDialog, InlineAlert), `05_*.md` 관리 틀
> 경로: `/admin/crawl` (수집 관리), `/admin/crawl/:jobId` (수집 진행/결과 — 같은 화면이 상태에 따라 진행 모드·결과 모드) — 신규

---

# 화면 A: 수집 관리 — `/admin/crawl`

## 레이아웃 (데스크톱, 본문 1200px)
```
┌──────────────────────────────────────────────────────────────────────┐
│ 관리 › 수집 관리                                                       │
│ ⬇ 수집 관리                                                           │
│                                                                      │
│ ┌ 수집 대상 등록 ────────────────────────────────────────────────────┐ │
│ │ IMSLP 작품 페이지 주소를 한 줄에 하나씩 붙여넣어 주세요                  │ │
│ │ ┌────────────────────────────────────────────────────────────────┐│ │
│ │ │ https://imslp.org/wiki/Piano_Sonata_No.14,_Op.27_No.2_(Beethoven,_Ludwig_van) ││ │  textarea 8줄, mono
│ │ │ https://imslp.org/wiki/Nocturnes,_Op.9_(Chopin,_Frédéric)      ││ │
│ │ │ …                                                              ││ │
│ │ └────────────────────────────────────────────────────────────────┘│ │
│ │ 51줄                                                    [확인]     │ │
│ │ ☑ 파일도 함께 받기                                                  │ │  체크박스 (기본 켬)
│ │   끄면 판본 정보만 가져오고 PDF는 받지 않아요 — 나중에 판본별 '파일 받아오기'로 받을 수 있어요 │ │
│ │                                                                    │ │
│ │ ┌──────────────────────────────────────────────────────────────┐  │ │  확인 후 판정 표
│ │ │ # │ 주소                                   │ 판정             │  │ │
│ │ ├───┼────────────────────────────────────────┼─────────────────┤  │ │
│ │ │ 1 │ …Piano_Sonata_No.14…                   │ ● 수집 예정       │  │ │
│ │ │ 2 │ …Fuer_Elise…                           │ ● 수집 예정       │  │ │
│ │ │   │                                        │   등록된 곡에 판본 붙임 │  │ │  ATTACH 보조 문구
│ │ │ 3 │ …Nocturnes,_Op.9…                      │ ○ 이미 있음 — 건너뜀 ☐ 정보만 다시 가져오기 │
│ │ │ 4 │ https://example.com/foo                │ ✕ 주소 형식 오류   │  │ │
│ │ │ 5 │ …Piano_Sonata_No.14… (1번과 같음)        │ – 중복 제거       │  │ │
│ │ └──────────────────────────────────────────────────────────────┘  │ │
│ │ 수집 예정 48건 (새 곡 30 · 등록된 곡에 붙임 18) · 건너뜀 2건 · 오류 1건 · 예상 소요 약 1시간 10분 │ │
│ │                                                                    │ │
│ │ ⓘ IMSLP 서버에 부담을 주지 않도록 요청 사이에 2초 이상 쉬며 천천히 가져옵니다. │ │  고정 안내 (항상 표시)
│ │   곡당 보통 1~2분, 파일이 크면 더 걸립니다. 화면을 닫아도 수집은 계속됩니다. │ │
│ │                                                    [수집 시작]     │ │
│ └────────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│ 수집 작업 목록                                                         │
│ ┌──────────────────────────────────────────────────────────────────┐ │
│ │ 상태      │ 대상 │ 성공 │ 실패 │ 건너뜀 │ 시작            │ 걸린 시간 │      │ │
│ ├──────────┼─────┼─────┼─────┼───────┼───────────────┼─────────┼──────┤ │
│ │ ● 진행 중  │  50 │  11 │   1 │   0   │ 09-06 14:02    │ –       │ 보기 ›│ │
│ │ ● 완료    │  50 │  46 │   3 │   1   │ 09-05 10:11    │ 1시간 3분 │ 보기 ›│ │
│ │ ● 중지됨  │  20 │   8 │   0 │   0   │ 09-04 18:40    │ 12분    │ 보기 ›│ │
│ └──────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────┘
```
**모바일**: 판정 표 → 카드(주소는 `…` 로 앞부분 생략, 끝 40자 표시). 작업 목록 → 카드(상태 · 성공/실패/건너뜀 · 시작). 버튼 100%.

## 구성 요소

| 요소 | 문구 / 동작 | 토큰 / 재사용 |
|---|---|---|
| 제목 | `cloud_download` + `수집 관리` | 기존 `.page-header` |
| 등록 패널 | 흰 카드 제목 `수집 대상 등록` | `.panel` 기존 |
| 안내 라벨 | `IMSLP 작품 페이지 주소를 한 줄에 하나씩 붙여넣어 주세요` | `.form-label` |
| 주소 입력 | `.form-textarea` 8줄, `--font-family-mono` `--font-size-sm`, 자리 문구 `https://imslp.org/wiki/Piano_Sonata_No.14,_Op.27_No.2_(Beethoven,_Ludwig_van)`. 아래 왼쪽 줄 수 `51줄`(빈 줄 제외). 파일 업로드 없음(1차) | |
| `파일도 함께 받기` | 체크박스, **기본 켬** (API `fetchFiles`, 01 §9-1). 라벨 `파일도 함께 받기`. 아래 도움말(`--font-size-xs` muted): `끄면 판본 정보만 가져오고 PDF는 받지 않아요 — 나중에 판본별 '파일 받아오기'로 받을 수 있어요`. **값을 바꾸면 이미 받은 판정 표의 예상 소요 시간이 낡은 것이므로 `확인`을 다시 눌러야 한다** — 바꾼 즉시 요약 줄의 예상 소요를 `다시 확인해 주세요`(warning 색)로 바꾸고 `수집 시작`을 disabled. 확인·시작 요청 모두에 이 값을 보낸다 | 기존 체크박스 스타일 |
| `확인` | `.btn.btn-secondary`. 입력이 비면 disabled. 클릭 → 서버 판정(`fetchFiles` 포함) → 판정 표. 확인 중엔 `확인 중…` | |
| 판정 표 | 열: `#` / 주소(mono, 긴 주소는 가운데 `…` 생략, hover title 전체) / 판정. 판정 값과 색: `수집 예정`(NEW, `--color-success`, `check_circle`) / `수집 예정` + 보조 문구 `등록된 곡에 판본 붙임`(**ATTACH** — 곡은 있는데 수집된 판본이 없는 시드·직접 등록 곡. 같은 초록 아이콘, 보조 문구는 `--font-size-xs` muted 로 판정 아래 줄. 곡 이름을 링크로 → `/admin/works/{existingWorkId}` 새 탭) / `이미 있음 — 건너뜀`(EXISTS, `--color-text-muted`, `remove_circle_outline`) + 오른쪽 체크박스 `정보만 다시 가져오기`(켜면 판정이 `정보만 갱신` 으로 바뀜, 도움말 `이미 받은 파일은 다시 받지 않아요`) / `주소 형식 오류`(INVALID_URL, `--color-danger`, `error`) / `중복 제거`(DUPLICATE, `--color-text-disabled`, `content_copy`, 몇 번 줄과 같은지 `(1번과 같음)`) | DataTable |
| 요약 줄 | `수집 예정 48건 (새 곡 30 · 등록된 곡에 붙임 18) · 건너뜀 2건 · 오류 1건 · 예상 소요 약 1시간 10분`. 괄호는 ATTACH 가 1건 이상일 때만. 정보만 갱신이 있으면 `· 정보 갱신 1건` 추가. 예상 소요는 서버 `estimatedSeconds`(**"파일도 함께 받기" 값에 따라 달라진다** — 끄면 곡당 수 초, 켜면 곡당 1~2분). 시·분 표기: `약 1시간 10분` / 1시간 미만 `약 12분` / 1분 미만 `1분 이내`. 값이 없으면 `예상 소요 계산 중` | `--font-size-base` medium |
| 고정 안내 | InlineAlert info, **판정 전에도 항상 표시**: `IMSLP 서버에 부담을 주지 않도록 요청 사이에 2초 이상 쉬며 천천히 가져옵니다. 곡당 보통 1~2분, 파일이 크면 더 걸립니다. 화면을 닫아도 수집은 계속됩니다.` "파일도 함께 받기"를 **끈 상태**면 두 번째 문장을 `파일을 받지 않으므로 곡당 몇 초면 끝납니다.` 로 바꾼다 | 00 §3-11 |
| `수집 시작` | `.btn.btn-primary.btn-lg`. **확인을 거쳐 수집 예정(새 곡·등록된 곡에 붙임·정보 갱신) 1건 이상**일 때만 활성. 클릭 → 작업 생성(`fetchFiles` 포함) → `/admin/crawl/:jobId` 이동. 입력이나 "파일도 함께 받기"를 고치면 판정 표가 낡은 것이므로 버튼 다시 disabled + `다시 확인해 주세요` | |
| 작업 목록 | 최근 순. 열: 상태 점+문구(`진행 중` `일시 정지` `완료` `중지됨` `실패`, 색은 05 관리 홈과 동일) / 대상 / 성공 / 실패 / 건너뜀 / 시작(`MM-DD HH:mm`) / 걸린 시간(`1시간 3분`, 진행 중 `–`) / `보기 ›`. 행 클릭 → 진행/결과 | DataTable, 20개 Pagination |

## 상태별 UI

| 상태 | 표시 |
|---|---|
| 로딩 | 등록 패널 즉시. 작업 목록 스켈레톤 3줄 |
| 작업 목록 실패 | 목록 자리 `연결을 확인해 주세요` + `다시 시도`. 등록 패널은 살아 있음 |
| 작업 0건 | 목록 자리 `아직 수집한 적이 없어요` (muted) |
| **진행 중 작업 있음** | `수집 시작` disabled + 옆 InlineAlert warning `진행 중인 수집이 끝나면 시작할 수 있어요 — 보기`("보기" → 그 작업). 확인(판정)은 가능. 상단 AdminBanner 도 표시 |
| 입력 비어 있음 | `확인` disabled, `수집 시작` disabled |
| 판정 결과 전부 오류/건너뜀 | 요약 `수집 예정 0건 …` + `수집 시작` disabled + `수집할 주소가 없어요` |
| 확인 실패(서버) | 입력 아래 InlineAlert danger `주소를 확인하지 못했어요. 잠시 후 다시 시도해 주세요` |
| 시작 실패 | `수집 시작` 아래 InlineAlert danger `수집을 시작하지 못했어요. 잠시 후 다시 시도해 주세요` / 409(동시 실행) → 진행 중 안내와 동일 |
| 세션 만료 | 공통 규칙. 붙여넣은 주소는 잃을 수 있음 안내 |

---

# 화면 B: 수집 진행 / 결과 — `/admin/crawl/:jobId`

작업 상태에 따라 같은 화면이 **진행 모드**(진행 중·일시 정지·중지됨) 또는 **결과 모드**(완료·실패)로 보인다.

## 레이아웃 — 진행 모드
```
┌──────────────────────────────────────────────────────────────────────┐
│ 관리 › 수집 관리 › 수집 작업 #12                                        │
│ ⬇ 수집 진행                                        [새로고침] [중지]   │
│                                                                      │
│ ┌────────────────────────────────────────────────────────────────┐   │
│ │ ● 진행 중                                                       │   │
│ │ ████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  12 / 50      │   │  ProgressBar (성공 primary, 실패 danger 이어붙임)
│ │ 12 / 50 완료 · 성공 11 · 실패 1 · 남은 예상 시간 약 45분            │   │
│ │ 지금 처리 중: Nocturne in E-flat major, Op.9 No.2 — 파일 받는 중 (2/2) │   │
│ │ 10초마다 자동으로 갱신돼요 · 마지막 갱신 14:23:10                     │   │
│ └────────────────────────────────────────────────────────────────┘   │
│                                                                      │
│ 항목 (50)     [전체 ▾]                                                │
│ ┌────────────────────────────────────────────────────────────────┐   │
│ │ # │ IMSLP 주소                    │ 상태     │ 결과                  │   │
│ ├───┼──────────────────────────────┼─────────┼──────────────────────┤   │
│ │ 1 │ …Piano_Sonata_No.14…          │ ● 성공   │ 판본 14개, 파일 2개 받음 → 곡 보기 │
│ │ 2 │ …Fuer_Elise…                  │ ● 실패   │ IMSLP에 그 페이지가 없어요        │
│ │ 3 │ …Nocturnes,_Op.9…             │ ◐ 처리 중 │ 파일 받는 중 (2/2)                │
│ │ 4 │ …                             │ ○ 대기   │                                  │
│ └────────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────┘
```

## 레이아웃 — 결과 모드
```
┌──────────────────────────────────────────────────────────────────────┐
│ ⬇ 수집 결과                                    [실패한 것만 다시 시도]  │
│ ┌────────────────────────────────────────────────────────────────┐   │
│ │ ● 완료                                                          │   │
│ │ 총 50건 — 성공 46 · 실패 3 · 건너뜀 1 · 걸린 시간 1시간 3분           │   │
│ │ 시작 09-06 14:02 · 종료 09-06 15:05                               │   │
│ └────────────────────────────────────────────────────────────────┘   │
│                                                                      │
│ ⚠ 성공한 곡은 한국어 제목·별칭·난이도가 비어 있어 '보완 필요' 상태예요 → 곡 관리에서 보완하기 │
│                                                                      │
│ 실패 (3)                                                             │  실패를 위로 모음
│ ┌────────────────────────────────────────────────────────────────┐   │
│ │ 2 │ …Fuer_Elise…       │ ● 실패 │ IMSLP에 그 페이지가 없어요                 │
│ │ 17│ …Etude_Op.10…      │ ● 실패 │ 파일을 받다가 끊겼어요 → 곡 보기 (파일 받아오기로 재시도) │
│ │ 33│ …Symphony_No.5…    │ ● 숨김 │ 피아노 독주곡이 아닌 것 같아요 → 곡 보기     │
│ └────────────────────────────────────────────────────────────────┘   │
│ 성공 (46) · 건너뜀 (1)                                                 │
│ ┌────────────────────────────────────────────────────────────────┐   │
│ │ …                                                                │   │
│ └────────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────┘
```
**모바일**: 요약 카드 그대로(문구 줄바꿈), 항목 표 → 카드(#·상태 / 주소 / 결과). 버튼 100%.

## 구성 요소

| 요소 | 문구 / 동작 | 토큰 / 재사용 |
|---|---|---|
| 제목 | 진행 모드 `수집 진행` / 결과 모드 `수집 결과`. 브레드크럼 `수집 작업 #{jobId}` | |
| 상태 표시 | 점 + 문구: `진행 중`(info) / `일시 정지`(warning) / `중지됨`(muted) / `완료`(success) / `실패`(danger). 색은 05 와 동일 | `--font-size-lg` medium |
| 진행 막대 | ProgressBar: 완료(성공+실패+건너뜀)/전체. 성공 구간 `--color-primary`, 실패 구간 `--color-danger` 이어붙임. 오른쪽 `12 / 50` | 00 §3-14 |
| 진행 요약 | `12 / 50 완료 · 성공 11 · 실패 1 · 남은 예상 시간 약 45분`. 건너뜀 있으면 `· 건너뜀 1`. 예상 시간 없으면 그 부분 생략 | `--font-size-base` |
| 현재 처리 | `지금 처리 중: {원어 제목 또는 주소} — {단계}`. 단계 문구: `판본 정보 읽는 중` / `파일 받는 중 (2/2)` / `미리보기 만드는 중`. 처리 중 항목이 없으면(대기 사이) `다음 항목 준비 중` | `--font-size-md` `--color-text-secondary` |
| 자동 갱신 안내 | `10초마다 자동으로 갱신돼요 · 마지막 갱신 14:23:10`. 진행 모드에서만 폴링(10초). 결과 모드·중지됨은 폴링 없음 | `--font-size-xs` muted |
| `새로고침` | `.btn.btn-outline` `refresh`. 즉시 재조회, 누르면 아이콘 회전 1회 | |
| `중지` | `.btn.btn-danger` `stop`. 진행 중·일시 정지에서만. ConfirmDialog `수집을 중지할까요?` 본문 `지금 처리 중인 곡은 끝까지 마친 뒤 멈춰요. 남은 항목은 '대기'로 남고, 나중에 이어서 시작할 수 있어요` [계속 진행] [중지]. 요청 후 상태 `중지하는 중…`(버튼 disabled) → 서버가 중지됨으로 바꾸면 갱신 | |
| `이어서 시작` | `.btn.btn-primary` `play_arrow`. **중지됨** 상태에서만 `중지` 자리에. 클릭 → 대기 항목부터 재개, 진행 모드로. 다른 작업이 진행 중이면 disabled + `진행 중인 수집이 끝나면 시작할 수 있어요 — 보기` | |
| `실패한 것만 다시 시도` | `.btn.btn-primary` `replay`. 결과 모드 + 실패 1건 이상일 때만. ConfirmDialog `실패한 {N}건으로 새 수집 작업을 만들까요?` → 새 작업 생성 → 그 작업의 진행 화면으로. "피아노 독주곡이 아닌 것 같아요"(숨김 등록)는 재시도 대상에서 **제외**(이미 등록됨) | |
| 항목 필터 | `.filter-select`: `전체` / `실패` / `성공` / `처리 중·대기` / `건너뜀` | |
| 항목 표 | 열: `#`(입력 순서) / IMSLP 주소(mono, 생략 표시, title 전체) / 상태 / 결과. 상태 값·아이콘: `대기`(`radio_button_unchecked` muted) / `처리 중`(`sync` info, 회전) / `성공`(`check_circle` success) / `실패`(`error` danger) / `건너뜀`(`remove_circle_outline` muted) / `숨김`(`visibility_off` warning — 비피아노 등록) | DataTable. 진행 모드에서는 처리 중 행이 보이도록 자동 스크롤하지 않는다(사용자가 보던 위치 유지) |
| 결과 열 — 성공 | `판본 14개, 파일 2개 받음 → 곡 보기`("곡 보기" → `/admin/works/:id`). 작업이 "파일도 함께 받기" 꺼짐(`fetchFiles: false`)이면 `판본 14개 (파일은 받지 않음) → 곡 보기`. ATTACH 모드면 `등록된 곡에 판본 14개 붙임, 파일 2개 받음 → 곡 보기`. 정보만 갱신이면 `판본 정보 갱신 (파일 재요청 없음) → 곡 보기` | |
| 결과 열 — 실패 사유 | 그대로: `IMSLP에 그 페이지가 없어요` / `작품 페이지에 PDF 판본이 없어요 → 곡 보기`(곡은 준비 중으로 등록됨) / `파일을 받다가 끊겼어요 → 곡 보기 (파일 받아오기로 재시도)` / `IMSLP가 응답하지 않아요` / `중단됨 — 서비스가 재시작됐어요` | `--color-text-secondary` |
| 결과 열 — 숨김 | `피아노 독주곡이 아닌 것 같아요 → 곡 보기 (확인 후 숨김 해제)` | |
| 결과 모드 요약 | `총 50건 — 성공 46 · 실패 3 · 건너뜀 1 · 걸린 시간 1시간 3분` + `시작 {일시} · 종료 {일시}` | |
| 결과 모드 안내 | InlineAlert warning(성공 1건 이상): `성공한 곡은 한국어 제목·별칭·난이도가 비어 있어 '보완 필요' 상태예요 → 곡 관리에서 보완하기`(링크 → `/admin/works?status=NEEDS_WORK`) | |
| 결과 모드 그룹 | `실패 (3)` 표를 위, `성공 (46) · 건너뜀 (1)` 표를 아래. 실패 0이면 실패 그룹 생략 | 소제목 `--font-size-lg` |

## 상태별 UI

| 상태 | 표시 |
|---|---|
| 로딩(첫 진입) | 요약 카드·표 스켈레톤 |
| 없는 jobId | `찾을 수 없는 페이지` |
| 불러오기 실패(첫 진입) | `연결을 확인해 주세요` + `다시 시도` |
| **자동 갱신 실패** (폴링 중) | 화면은 마지막 현황 유지. 진행 막대 옆 InlineAlert danger 한 줄 `현황을 못 가져왔어요 — 새로고침`("새로고침" 버튼). 다음 폴링이 성공하면 사라짐 |
| **일시 정지** (IMSLP 무응답 3회) | 상태 `일시 정지`(warning). 요약 아래 InlineAlert warning `IMSLP가 응답하지 않아요. 10분 뒤 자동으로 다시 시도합니다 (다음 시도 14:35)` + `.btn.btn-outline` `지금 이어서 시작`. 중지 버튼은 유지. 자동 재시도도 실패해 서버가 중지됨으로 바꾸면 중지됨 표시 + AdminBanner 문구 갱신 |
| **중지됨** | 상태 `중지됨`. 요약 `12 / 50 완료 · 성공 11 · 실패 1 · 대기 38`. 버튼 `이어서 시작`. 폴링 없음 |
| **서비스 재시작으로 중단** | 중지됨과 동일 + 처리 중이던 항목은 `실패 — 중단됨 — 서비스가 재시작됐어요`. InlineAlert info `서비스가 재시작되어 멈췄어요. '이어서 시작'하면 대기 항목부터 계속하고, 이미 받은 파일은 다시 받지 않아요` |
| **완료** | 결과 모드. `실패한 것만 다시 시도`는 실패 있을 때만 |
| **실패** (작업 자체 실패, 예: 0건 처리 후 중단) | 상태 `실패`(danger) + 사유(서버 문구) + `실패한 것만 다시 시도` |
| 중지 요청 실패 | 버튼 옆 InlineAlert danger `중지하지 못했어요 — 다시 시도` |
| 이어서 시작 실패 | 동일 패턴 `이어서 시작하지 못했어요 — 다시 시도` / 409 → 진행 중 안내 |
| 브라우저 닫았다 다시 진입 | 별도 처리 없음 — 서버 현황을 그대로 그림(요구사항 충족은 서버 책임) |
| 관리 홈 띠 | 이 화면에서도 AdminBanner 표시(중복이지만 규칙 단순화). 단 이 화면에서는 "보기" 링크 생략 |

---

## 플랫폼 차이 (07 전체)

- 앱 미도입.
- 모바일: 주소 textarea 6줄, 판정 표·항목 표는 카드, 진행 요약은 줄바꿈 허용, 버튼(중지·이어서·재시도) 폭 100% 하단 sticky.
- 데스크톱: 표, 버튼은 제목 오른쪽.
