package com.test.test.sheetmusic.work;

/**
 * 보완 필요 항목 — 계산값 (01_ERD §4). 순서가 계약이다(02 §4-7: 화면이 이 순서로 이어 붙인다).
 * 규칙은 {@link WorkNeedsWork} 한 곳에만 있다.
 */
public enum WorkMissing {
    TITLE_KO, ALIAS, LEVEL, RECOMMENDED_EDITION, COPYRIGHT_JUDGMENT
}
